import { Box, Text } from '@mantine/core';

/**
 * Page d'accueil de l'application Carmen.
 *
 * Placeholder — le contenu sera implémenté ultérieurement.
 *
 * @returns {JSX.Element} Zone de contenu principale vide.
 */
export function HomePage() {
  return (
    <Box p="md">
      <Text c="dimmed">Accueil — contenu à venir</Text>
    </Box>
  );
}
